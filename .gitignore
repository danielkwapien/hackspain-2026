# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Data artefacts (the compressed originals in datasets/ are tracked)
datasets/transactions.csv
datasets/invoices.csv
*.duckdb
*.duckdb.wal
*.parquet

# OS / editors
.DS_Store
.idea/
.vscode/

# Data scratch
/data

# Node / pnpm (dashboard en app/)
node_modules/
dist/
.vite/
coverage/
*.log
.env
.env.*
*.zip

# Local planning (parent session), never committed
plans/


AGENTS.md

# Loop harness markers (opt-in gate)
.loop-on
.gate-on
.gate-fails

# Generated scoring artefacts (regenerable from the pipelines)
core/outputs/scores_embat.json
core/outputs/evaluation.json
core/.cache/

# Salidas generadas por los pipelines de core/ (se regeneran en segundos)
core/outputs/

# Notas locales de mejora (contexto para sesiones nuevas), nunca se commitean
IMPROVES.md
improves.md
