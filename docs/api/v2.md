# API v2 — el motor X-Ray servido desde `datasets_mocked/`

Estado: implementado en `app/api/src/v2/` (XR-001, fase B). Complementa a v1
(`docs/dani/contrato-dashboard-v1.md`), **no lo sustituye**: `/health` y `/api/v1/*`
siguen igual.

La API **no calcula finanzas**. Todo lo que devuelve v2 está precalculado en las tablas de
`datasets_mocked/` (score, pilares, señales, drivers, régimen, outlook, alertas,
narrativas, grupos, frames). Aquí solo se lee, se filtra, se ordena y se pagina.

---

## 1. De dónde salen los datos

| Variable | Valor por defecto | Para qué |
|---|---|---|
| `EXPORTS_DIR` | `app/exports/v1` | inventario v1 (manda, igual que antes) |
| `XRAY_V2_DIR` | `EXPORTS_DIR/../..` | tablas v2 (`score_timeline.csv`, `signals.csv`, `frames/`, …) |

Con `EXPORTS_DIR=datasets_mocked/exports/v1`, `V2_DIR` se resuelve a `datasets_mocked/` y
v2 queda servido sin tocar nada más:

```bash
EXPORTS_DIR=$(pwd)/datasets_mocked/exports/v1 corepack pnpm --filter api dev
```

Con el inventario real (`app/exports/v1` → `V2_DIR = app/`) no hay `manifest.json` de
motor: **todos los `/api/v2/*` responden 503** y `/health` responde exactamente lo de
siempre (`engine: "pending"`, sin `data_kind`).

CORS, puerto y logger son los de v1 (`http://localhost:5173` y `:4173`, puerto `PORT` o
8787).

### `/health` con el mock

```jsonc
{ "status": "ok", "contract_version": "dashboard-v1", "dataset_version": "embat-v2",
  "cutoff_date": "2026-09-01", "generated_at": "2026-09-19T00:00:00+00:00",
  "engine": "mock", "data_kind": "mock" }
```

`engine: "mock"` y `data_kind` **solo** aparecen cuando el `manifest.json` de `V2_DIR`
lleva `data_kind: "mock"`; el valor sale del manifiesto, nunca está escrito en el código.

### Arranque en frío (dataset completo, 1.286 empresas × 24 meses)

Las tablas se cargan bajo demanda, una vez, en la **primera petición** que las necesita
(incluida `/health`), no al arrancar el proceso:

| Momento | Tiempo | RSS del proceso |
|---|---|---|
| Solo v1 | — | 78 MB |
| 1.ª petición v2 (`/api/v2/meta`): carga 9 tablas + manifiesto, 25 MB de CSV | **0,28 s** | 247 MB |
| Peticiones v2 siguientes (`/meta`, `/universe`, `/companies/:id`…) | 1–6 ms | — |
| 1.ª petición de `/companies/:id/signals`: lee e indexa `signals.csv` (438.701 filas, 80 MB) | **0,50 s** | 446 MB |
| `/signals` siguientes | ~1 ms | — |

`signals.csv` se carga **perezosamente** a propósito: cargarlo con las demás multiplicaba
por dos el arranque y la memoria de endpoints que no usan señales. `frames/*.json` no se
precargan nunca: se leen del disco por mes.

El parser CSV es propio (`src/v2/csv.ts`, sin dependencias nuevas) porque las tablas llevan
texto entrecomillado con comas dentro (`value_fmt`, `message`, `body`, `anchors`): un
`split(",")` las partiría. Está verificado fila a fila contra el módulo `csv` de Python
sobre las nueve tablas del dataset completo.

---

## 2. Reglas transversales

- Todos los endpoints son `GET` y devuelven JSON.
- `as_of` por defecto = **último mes disponible** (`2026-08`). Debe existir en el dataset.
- Validación de dominio cerrado → `400 {error:"invalid_query", message}` diciendo qué
  valores valen. Ej.: `sort inválido: ebitda. Válidos: score, delta_1m, delta_3m`.
- Id mal formado → `400 {error:"invalid_company_id"|"invalid_group_id", message}`.
- Entidad inexistente → `404 {error:"company_not_found"|"group_not_found"|"frame_not_found"|"month_not_found", message}`.
- Sin tablas v2 → `503 {status:"no_v2_tables", message, hint}` (mismo patrón que
  `sendNoExports` de v1).
- `limit` entre 1 y **500** (defecto 50); `offset` ≥ 0 (defecto 0).

Dominios cerrados:

| Parámetro | Valores |
|---|---|
| `unit` | `company`, `group` |
| `sort` (universe) | `score`, `delta_1m`, `delta_3m` |
| `order` | `asc`, `desc` |
| `band` | `solid`, `healthy`, `watch`, `stress` |
| `regime` | `warmup`, `stable`, `improving`, `deteriorating`, `blip`, `shock_pending`, `recovering` |
| `pillar` | `L`, `P`, `C`, `D`, `A` |
| `group_by` | `group`, `country`, `erp` |
| `metric` | `delta_3m`, `delta_1m`, `score` |
| `size_by` | `op_in_12m`, `n_companies` |
| `severity` | `watch`, `review`, `urgent` |
| `direction` | `down`, `up` |
| `as_of`, `month` | `YYYY-MM` **existente** en el dataset |
| `from`, `to`, `since`, `until` | `YYYY-MM` (solo formato; recortan, no fallan si caen fuera) |

Los ejemplos de `docs/api/examples/*.json` son respuestas **reales del dataset completo**
con los arrays recortados a tres elementos (salvo `sparkline_12`, `months`, `anchors`,
`strength_flags`, `countries` y `currencies`, que van enteros).

---

## 3. Endpoints

### `GET /api/v2/universe`

El universo en un mes: una fila por empresa (o por grupo) con score, delta, régimen y
sparkline. Es la tabla principal de la UI.

| Parámetro | Dominio | Defecto |
|---|---|---|
| `as_of` | mes del dataset | último mes |
| `unit` | `company` \| `group` | `company` |
| `sort` | `score` \| `delta_1m` \| `delta_3m` | `score` |
| `order` | `asc` \| `desc` | `desc` |
| `band`, `regime` | dominio cerrado | sin filtro |
| `q` | texto libre (id o nombre, sin distinguir mayúsculas) | sin filtro |
| `group_id` | `GROUP_\d{4}` | sin filtro |
| `limit`, `offset` | 1–500 / ≥ 0 | 50 / 0 |

Errores: `400` por cualquier parámetro fuera de dominio; `503` sin tablas.

```jsonc
{ "items": [ { "id": "COMP_0821", "name": "Grupo Ulzama y Cia. S.L.", "group_id": "GROUP_0070",
    "score": 97.39, "band": "solid", "delta_1m": 0.06, "delta_3m": 0.41, "regime": "stable",
    "outlook_label": "positive", "confidence": 0.81, "branch": "no_debt",
    "op_in_12m": 14173184.98, "sparkline_12": [ /* 12 meses hasta as_of */ ], "alert": false } ],
  "total": 1286, "as_of": "2026-08", "unit": "company", "limit": 50, "offset": 0,
  "data_kind": "mock" }
```

- `sparkline_12`: los 12 últimos meses **hasta `as_of`**; menos puntos si la empresa tiene
  menos historia (una empresa que arranca en 2026-01 trae 8).
- `alert`: hay alerta detectada en `as_of` para esa entidad.
- Con `unit=group` los items cambian: `group_id: null`, `op_in_12m_eur`,
  `n_companies_scored`, `dispersion`, `weakest_company` y `outlook_label: null`
  (`group_timeline.csv` no publica etiqueta de outlook).

Ejemplo completo: [`examples/universe.json`](examples/universe.json).

### `GET /api/v2/companies/:id`

El vector completo de una empresa en `as_of` (forma de ENGINE §11.3).

| Parámetro | Dominio | Defecto |
|---|---|---|
| `:id` | `COMP_\d{4}` | — |
| `as_of` | mes del dataset | último mes |

Errores: `400 invalid_company_id`; `404 company_not_found`; `404 month_not_found` si la
empresa existe pero no estaba activa en `as_of`; `503` sin tablas.

```jsonc
{ "company": { /* fila de companies.csv: identificación, cobertura real, op_in_12m */ },
  "as_of": "2026-08", "score": 58.99, "band": "watch",
  "delta_1m": 0.54, "delta_3m": -1.08, "delta_6m": -5.84,
  "regime": "stable", "confidence": 1, "branch": "full", "warmup": false,
  "outlook": {"h3": 57.23, "h6": 55.17, "low": 45.76, "high": 64.57, "label": "negative"},
  "pillars": {"L": {"value": 0.46, "weight": 0.25}, "P": {…}, "C": {…}, "D": {…}, "A": {…}},
  "strength_flags": ["GROWTH_NO_DSO", "DELEVERAGING"],
  "timeline": [{"month": "2024-12", "score": 65.28, "band": "healthy", "regime": "warmup",
                "outlook_low": 55.87, "outlook_high": 74.69}],
  "drivers": [{"rank": 1, "signal_id": "P1", "pillar": "P", "contribution": -1.66,
               "delta_vs_prev": -0.37, "value": 0.56,
               "value_fmt": "56 % de las facturas propias pagadas tarde", "direction": "worse"}],
  "penalty": {"points": 0.88, "weakest_pillar": "P"},
  "cap": null,
  "alert": { /* fila de alerts.csv, o null */ },
  "narrative": {"headline": "…", "body": "…", "watch_next": "…", "guardrail_passed": true},
  "audit": {"data_kind": "mock", "params_version": "v1", "model_version": "mock-v1",
            "data_version": "embat-v2", "generator_version": "mock-gen-1", "seed": 42,
            "generated_at": "2026-09-19T00:00:00+00:00"} }
```

- `timeline` trae **todos** los meses activos de la empresa (no solo hasta `as_of`).
- `cap`: `null` cuando no hay techo; `{code, value}` cuando sí (`LOCFULL`, `NEGCASH`,
  `SSMISS`, `DEBTSTOP`).
- `penalty.points` es **positivo** (puntos restados, como en `score_timeline.csv`) y
  `weakest_pillar` es `null` cuando no hay penalización.
- `alert` es la última alerta con `month_detected <= as_of`, o `null`.
- `drivers` incluye las filas `PENALTY` y `CAP` de `drivers.csv` cuando existen.

Ejemplo completo: [`examples/company.json`](examples/company.json).

### `GET /api/v2/companies/:id/signals`

Las 28 señales agrupadas por pilar, con su serie histórica.

| Parámetro | Dominio | Defecto |
|---|---|---|
| `as_of` | mes del dataset | último mes |
| `pillar` | `L` \| `P` \| `C` \| `D` \| `A` | los cinco |

Errores: `400 invalid_company_id`, `400 invalid_query` (pillar/as_of), `404
company_not_found`, `404 month_not_found`, `503`.

```jsonc
{ "company_id": "COMP_1267", "as_of": "2026-08",
  "pillars": [ { "pillar": "L", "pillar_name": "Liquidez", "weight": 0.25, "value": 0.46,
    "signals": [ { "signal_id": "L1", "name": "Dias de colchon de caja", "unit": "dias",
      "value": 19.96, "value_fmt": "20 dias de colchon de caja", "u": 0.47, "u_smooth": 0.41,
      "u_ref": 0.62, "weight": 0.075, "contribution": -1.49, "delta_vs_prev": 0.41,
      "is_available": true, "quality_flag": null,
      "series_24m": [{"month": "2024-12", "value": 33.41, "u": 0.66}] } ] } ] }
```

- `series_24m`: todos los meses **hasta `as_of`** (24 como máximo; menos si la empresa
  tiene menos historia).
- Una señal que la empresa no puede tener **no aparece**: `signals.csv` solo trae filas
  disponibles, así que `is_available` es siempre `true`. Una empresa sin línea de crédito
  no trae `D1`, una sin facturas no trae `P1…P3`/`C1…C6`.
- `weight` es el peso efectivo de la señal en el score total (pilar × señal renormalizado);
  `contribution` son puntos: `100 · weight · (u_smooth − u_ref)`.

Ejemplo completo: [`examples/company-signals.json`](examples/company-signals.json).

### `GET /api/v2/companies/:id/timeline`

La serie mensual completa, con nivel, penalización, techo y banda de outlook. Devuelve un
**array plano** (sin envoltorio).

| Parámetro | Dominio | Defecto |
|---|---|---|
| `from`, `to` | `YYYY-MM` | toda la historia activa |

Errores: `400 invalid_company_id`, `400 invalid_query` (from/to mal formados), `404
company_not_found`, `503`.

```jsonc
[ {"month": "2026-06", "score": 59.96, "level": 59.96, "penalty": 0.60, "cap": 100,
   "cap_code": null, "band": "watch", "regime": "stable", "delta_1m": -0.11,
   "outlook_3m": 57.95, "outlook_6m": 55.44, "outlook_low": 46.03, "outlook_high": 64.85,
   "confidence": 1} ]
```

`cap` es 100 cuando no hay techo; cuando lo hay, `cap` baja (p. ej. 60 con
`cap_code: "LOCFULL"`) y `score = min(level, cap)`.

Ejemplo completo: [`examples/company-timeline.json`](examples/company-timeline.json).

### `GET /api/v2/groups/:id`

El grupo consolidado en `as_of` más sus filiales con el resumen de universe.

| Parámetro | Dominio | Defecto |
|---|---|---|
| `:id` | `GROUP_\d{4}` | — |
| `as_of` | mes del dataset | último mes |

Errores: `400 invalid_group_id`, `404 group_not_found`, `503`.

```jsonc
{ "group": {"group_id": "GROUP_0095", "name": "Ulzama Participaciones", "erp": null,
            "n_companies": 12, "countries": [], "currencies": ["EUR"],
            "consolidation_currency": "EUR", "op_in_12m_eur": 26233293.89,
            "has_intercompany": true},
  "as_of": "2026-08", "score": 70.41, "band": "healthy", "regime": "shock_pending",
  "delta_1m": -0.56, "delta_3m": 1.84, "outlook_6m": 66.90, "outlook_low": 57.50,
  "outlook_high": 76.31, "confidence": 0.98, "n_companies_scored": 12, "dispersion": 39.20,
  "weakest_company": "COMP_1051", "weakest_score": 41.97,
  "strongest_company": "COMP_0248", "strongest_score": 81.17,
  "intragroup_dependency_max": 0.50, "alert": false,
  "timeline": [{"month": "2024-12", "score": 68.39, "band": "healthy", "regime": "warmup",
                "delta_1m": null, "outlook_low": 58.99, "outlook_high": 77.80,
                "dispersion": 31.07, "n_companies_scored": 8}],
  "companies": [ /* items de universe, ordenados por score desc */ ] }
```

`strongest_score` se resuelve mirando el score de `strongest_company` en `as_of`
(`group_timeline.csv` publica el id pero no su score).

Ejemplo completo: [`examples/group.json`](examples/group.json).

### `GET /api/v2/alerts`

La bandeja de alertas, ya con presupuesto y cool-down aplicados por el generador.

| Parámetro | Dominio | Defecto |
|---|---|---|
| `since`, `until` | `YYYY-MM` (sobre `month_detected`, inclusive) | sin filtro |
| `severity` | `watch` \| `review` \| `urgent` | sin filtro |
| `direction` | `down` \| `up` | sin filtro |
| `company_id` | `COMP_\d{4}` | sin filtro |
| `group_id` | `GROUP_\d{4}` | sin filtro |
| `limit`, `offset` | 1–500 / ≥ 0 | 50 / 0 |

```jsonc
{ "items": [ {"alert_id": "ALERT_00002", "company_id": "COMP_0835", "group_id": "GROUP_0250",
    "event": "cap_NEGCASH", "severity": "urgent", "direction": "down",
    "month_detected": "2025-04", "month_evident": "2025-05", "lead_time_months": 1,
    "trigger_signal": "NEGCASH", "score_before": 58.74, "score_after": 40,
    "status": "resolved",
    "message": "caja agregada en negativo dos meses seguidos: el score cae 18,7 puntos…"} ],
  "total": 69, "limit": 50, "offset": 0 }
```

Los items salen tal cual de `alerts.csv`, ordenados por `alert_id` (que es cronológico).

Ejemplo completo: [`examples/alerts.json`](examples/alerts.json).

### `GET /api/v2/treemap`

Agregación por grupo, país o ERP para el treemap: tamaño y color por empresa, y la media
ponderada por tamaño ya calculada en el servidor.

| Parámetro | Dominio | Defecto |
|---|---|---|
| `as_of` | mes del dataset | último mes |
| `group_by` | `group` \| `country` \| `erp` | `group` |
| `metric` | `delta_3m` \| `delta_1m` \| `score` | `delta_3m` |
| `size_by` | `op_in_12m` \| `n_companies` | `op_in_12m` |

```jsonc
{ "as_of": "2026-08", "group_by": "group", "metric": "delta_3m", "size_by": "op_in_12m",
  "groups": [ { "key": "GROUP_0126", "label": "Bierzo Holding", "value_sum": 28890006406.22,
    "delta": 1.71,
    "items": [ {"id": "COMP_1185", "name": "Comercial Navarro y Cia. S.L.",
                "size": 28775890156, "color_value": 1.71, "score": 57.92,
                "band": "watch"} ] } ] }
```

- `delta` = media de `metric` ponderada por `size`. La UI no la recalcula.
- `value_sum` = suma de `size`. Con `group_by=country|erp`, las empresas sin dato caen en
  la clave `unknown`.
- **Cuidado**: `op_in_12m` está en la **moneda contable de cada empresa** (`companies.csv`),
  así que sumarlo mezcla divisas. Para comparar grupos en EUR está `op_in_12m_eur` en
  `/api/v2/universe?unit=group`.

Ejemplo completo: [`examples/treemap.json`](examples/treemap.json).

### `GET /api/v2/frames` y `GET /api/v2/frames/:month`

El replay: la lista de meses y el estado completo del universo en uno de ellos. Los frames
no se precargan; se leen del disco por mes.

Errores: `400` si `:month` no es `YYYY-MM`; `404 frame_not_found` si no hay frame de ese
mes; `503` sin tablas.

```jsonc
// GET /api/v2/frames
{ "months": ["2024-09", …, "2026-08"], "frames_url": "/api/v2/frames/{month}",
  "data_kind": "mock" }

// GET /api/v2/frames/2026-08  (el fichero frames/2026-08.json tal cual)
{ "month": "2026-08",
  "companies": [{"company_id": "COMP_0001", "score": 81.76, "delta_1m": 7.89,
                 "band": "solid", "regime": "stable", "sparkline_12": [...], "alert": false}],
  "groups": [{"group_id": "GROUP_0001", "score": 63.11, "band": "healthy",
              "delta_1m": 3.50, "regime": "stable", "dispersion": 3.01,
              "weakest_company": "COMP_0158", "alert": false}],
  "alerts_this_month": [ /* filas de alerts.csv detectadas ese mes */ ],
  "stats": {"mean_score": 60.44, "n_moving": 567, "n_deteriorating": 58, "n_improving": 27} }
```

Ejemplos: [`examples/frames.json`](examples/frames.json),
[`examples/frame-month.json`](examples/frame-month.json).

### `GET /api/v2/catalog/signals`

El catálogo de las 29 señales (28 puntúan; `A6` tiene `scores: false`), con anclas o
percentiles congelados y la mediana del universo.

```jsonc
{ "items": [ {"signal_id": "L1", "pillar": "L", "pillar_name": "Liquidez",
    "name": "Dias de colchon de caja", "unit": "dias", "direction": "higher_better",
    "weight_in_pillar": 30, "pillar_weight": 25, "norm": "anchor",
    "anchors": [[0, 0], [10, 0.3], [27, 0.6], [60, 0.9], [120, 1]],
    "window": "3m", "ewma": "flow", "requires": null, "scores": true,
    "breakpoints": null, "u_ref": 0.6194} ],
  "total": 29, "pillar_weights": {"L": 25, "P": 20, "C": 15, "D": 20, "A": 20} }
```

`anchors` para `norm: "anchor"`; `breakpoints` (21 cortes p1…p99, del `manifest.json`) para
`norm: "percentile"`. `u_ref` es la mediana del universo con la que se calcula
`contribution`.

Ejemplo completo: [`examples/catalog-signals.json`](examples/catalog-signals.json).

### `GET /api/v2/meta`

La procedencia del dataset servido: versiones, semilla, recuentos y hashes de los CSV de
origen. Es lo que la UI necesita para pintar el banner de «datos simulados».

```jsonc
{ "data_kind": "mock", "contract_version": "dashboard-v1", "model_version": "mock-v1",
  "data_version": "embat-v2", "params_version": "v1", "generator_version": "mock-gen-1",
  "seed": 42, "limit": null, "generated_at": "2026-09-19T00:00:00+00:00",
  "cutoff_date": "2026-09-01", "window": {"start": "2024-09", "end": "2026-08"},
  "months": ["2024-09", …], "counts": {"companies.csv": 1286, "signals.csv": 438701, …},
  "hashes": [{"file": "companies.csv", "sha256": "5496ad…", "bytes": 70591}],
  "notes": ["Entidades y cobertura REALES (datasets/); scores, senales y alertas sinteticos…"] }
```

Ejemplo completo: [`examples/meta.json`](examples/meta.json).

---

## 4. Desviaciones respecto a PLAN §4.2 y ENGINE §11.3

| Qué | Decisión |
|---|---|
| ENGINE §11.3 llama `signal` al campo del driver | v2 usa **`signal_id`** en todos los endpoints (drivers, señales, catálogo, alertas) para no tener dos nombres del mismo campo |
| ENGINE §11.3 pone `penalty: {weakest_pillar, points: -7.5}` | `points` se sirve con el signo de `score_timeline.csv` (**positivo** = puntos restados) y `weakest_pillar` es el pilar con la nota más baja, `null` si no hay penalización |
| PLAN pide `cap` a secas | `cap` es `null` o `{code, value}`; en `/timeline` van las dos columnas crudas (`cap`, `cap_code`) |
| PLAN pide `{items, total}` en alerts | se añaden `limit` y `offset` (misma forma que v1) |
| PLAN pide `sort` en universe | `sort` ordena; `unit=group` usa la misma clave sobre `group_timeline.csv` |
| `/universe`, `/signals` | se añaden campos que ya están en las tablas y la UI va a pedir: `branch`, `op_in_12m`/`op_in_12m_eur`, `u_ref`, `weight`, `unit`, `pillar_name` |

## 5. Pendiente (campos del anexo §10 del plan que el dataset todavía no publica)

Nombres exactos, para que el motor real o una siguiente pasada del generador los añadan:

- `coverage_ratio` en los items de `/universe` y en las filiales de `/groups/:id`. Existe
  en `exports/v1/results/<id>.json` (`quality.coverage_ratio`) pero **no** en ninguna tabla
  v2; servirlo obligaría a abrir un fichero por empresa en cada listado.
- `intragroup_dependency` por filial en `/groups/:id` (hoy solo está el agregado
  `intragroup_dependency_max`; el detalle es la señal `A5`, que vive en `signals.csv`).
- `stats.mean_score_weighted` y `stats.mean_score_simple` en `/frames/:month`: el
  generador solo escribe `stats.mean_score`.
- `top_driver: {signal_id, value_fmt, contribution}` en las alertas (`/alerts` y
  `alerts_this_month[]` de los frames): `alerts.csv` solo trae `trigger_signal`.
- `p5` y `p95` del universo por señal en `/catalog/signals`: el `manifest.json` congela los
  21 cortes de percentil y `u_ref`, pero no p5/p95.

## 6. Verificación

```bash
cd app && corepack pnpm --filter api exec vitest run v2   # endpoints v2 sobre las fixtures
cd app && corepack pnpm --filter api exec vitest run app  # v1 intacto
cd app && corepack pnpm typecheck
```

Las fixtures de `app/api/test/fixtures/v2/` son un mock reducido y committeado (50
empresas, 44 grupos, 24 meses, 11 alertas), generado con:

```bash
.venv/bin/python datasets_mocked/generate_mock.py --seed 7 --limit 50 \
    --data-dir datasets --out app/api/test/fixtures/v2 \
    --inventory app/exports/v1 --now 2026-09-19T00:00:00+00:00
# y luego: rm -rf app/api/test/fixtures/v2/exports/v1/{results,companies}
```

`exports/v1/results/` y `exports/v1/companies/` se borran de la fixture (3,4 MB) porque v2
no los lee y los tests de v1 usan su propia fixture (`test/fixtures/exports/`). De
`exports/v1` solo se conservan `manifest.json`, `companies.json` y `groups.json`, que son
los que carga `loadExports` para `/health`.
