{
  "name": "api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "examples": "node scripts/gen-api-examples.mjs"
  },
  "dependencies": {
    "@duckdb/node-api": "1.4.4-r.1",
    "@fastify/cors": "^11.3.0",
    "fastify": "^5.12.5",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.23.13",
    "typescript": "~6.0.2",
    "vitest": "^5.0.1"
  }
}
