/** Fondo «Marino»: un solo orbe navy, sobrio y casi imperceptible. Solo la capa; el stage es común. */
export function Marino() {
  return (
    <div className="proto-bg" aria-hidden="true">
      <span className="proto-orb proto-orb-marino" />
    </div>
  );
}
