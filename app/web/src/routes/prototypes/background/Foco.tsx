/** Fondo «Foco»: un foco neutro cálido-blanco que solo respira, el más cercano al login de Trade Republic. */
export function Foco() {
  return (
    <div className="proto-bg" aria-hidden="true">
      <span className="proto-orb proto-orb-foco" />
    </div>
  );
}
