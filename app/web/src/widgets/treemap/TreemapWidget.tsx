/** Mapa por grupos: marcador de posición hasta que la unidad U4 ponga el cuerpo. */

import type { ComponentType } from "react";
import type { WidgetContentProps } from "@/widgets/registry";

export const TreemapWidget: ComponentType<WidgetContentProps> = () => null;
