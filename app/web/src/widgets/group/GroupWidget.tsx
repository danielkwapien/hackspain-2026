/** Grupo y filiales: marcador de posición hasta que la unidad U4 ponga el cuerpo. */

import type { ComponentType } from "react";
import type { WidgetContentProps } from "@/widgets/registry";

export const GroupWidget: ComponentType<WidgetContentProps> = () => null;
