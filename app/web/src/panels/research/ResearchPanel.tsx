/**
 * Panel Investigación: la empresa seleccionada sobre `/api/v2/companies/:id`.
 * Stub del marco (XR-030, item 4): el contenido llega con el item 7.
 */

import type { ReactElement } from "react";
import { EmptyState } from "@/components/states";

export function ResearchPanel(): ReactElement {
  return <EmptyState title="Selecciona una empresa" />;
}
