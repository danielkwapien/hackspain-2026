/**
 * Panel Comparativa: varias empresas en un `LineNoAxes` con normalización y rangos.
 * Stub del marco (XR-030, item 4): el contenido llega con el item 6.
 */

import type { ReactElement } from "react";
import { EmptyState } from "@/components/states";

export function ComparePanel(): ReactElement {
  return <EmptyState title="Añade empresas desde la tabla" />;
}
